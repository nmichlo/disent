# @package _group_
name: adavae
cls:
  target: disent.frameworks.vae.weaklysupervised.AdaVae
  params:
    beta: 4
    average_mode: 'gvae'  # gvae or ml-vae
    mse_shared_loss_weight: 0

augment_dataset:
  cls:
    target: disent.dataset.pairs.PairedVariationDataset
    params:
      k: ${dataset.k}