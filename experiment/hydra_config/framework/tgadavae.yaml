# @package _group_
name: tgadavae
cls:
  target: disent.frameworks.vae.supervised.TripletGuidedAdaVae
  params:
    beta: 4
    average_mode: 'gvae'       # [gvae, ml-vae]
    anchor_ave_mode: 'average' # [average, thresh]
    triplet_margin: 0.1
    triplet_scale: 1

augment_dataset:
  cls:
    target: disent.dataset.triplets.SupervisedTripletDataset
    params:
      k: ${dataset.k}
      n_k: ${dataset.n_k}