# @package _group_
name: badavae
cls:
  target: disent.frameworks.vae.supervised.BoundedAdaVae
  params:
    beta: 4
    average_mode: 'gvae'  # gvae or ml-vae

augment_dataset:
  cls:
    target: disent.dataset.triplets.SupervisedTripletDataset
    params:
      k: ${dataset.k}
      n_k: ${dataset.n_k}
      sample_nearby: FALSE