# @package _group_
name: mpi3d_toy
data:
  _target_: disent.dataset.ground_truth_data.data_mpi3d.Mpi3dData
  subset: 'toy'
  data_dir: ${dataset.data_dir}/mpi3d
  in_memory: ${dataset.try_in_memory}
  force_download: False
transforms:
  - _target_: torchvision.transforms.ToTensor
x_shape: [3, 64, 64]