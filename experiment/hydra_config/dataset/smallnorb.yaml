# @package _group_
name: smallnorb
cls:
  target: disent.dataset.ground_truth_data.data_norb.SmallNorbData
  params:
    data_dir: ${dataset.data_dir}/smallnorb
    force_download: False
    is_test: False
transforms:
  - target: torchvision.transforms.Resize
    params:
      size: 64
  - target: torchvision.transforms.ToTensor
x_shape: [1, 64, 64]