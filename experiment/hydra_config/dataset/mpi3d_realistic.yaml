# @package _group_
name: mpi3d_realistic
cls:
  target: disent.dataset.ground_truth_data.data_mpi3d.Mpi3dData
  params:
    subset: 'realistic'
    data_dir: ${dataset.data_dir}/mpi3d
    in_memory: ${dataset.try_in_memory}
    force_download: False
transforms:
  - target: torchvision.transforms.ToTensor
x_shape: [3, 64, 64]