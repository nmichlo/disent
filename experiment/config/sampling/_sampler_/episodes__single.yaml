name: episodes__single

sampler_cls:
  _target_: disent.dataset.sampling.RandomEpisodeSampler
  num_samples: 1
  # TODO: this needs to be updated to use the same API as ground_truth wrappers.
  sample_radius: 32
