# @package _group_
metric_list:
  - flatness:
  - flatness_components:
  - mig:

# these are the default settings, these can be placed in the list above
default_on_final: TRUE
default_on_train: TRUE
default_every_n_steps: 600
