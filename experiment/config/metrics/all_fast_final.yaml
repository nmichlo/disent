# @package _group_
metric_list:
  - flatness:             # pragma: delete-on-release
  - flatness_components:  # pragma: delete-on-release
  - mig:
  - sap:
  - unsupervised:
  - dci:
      every_n_steps: 3600
      on_final: FALSE
  - factor_vae:
      every_n_steps: 3600
      on_final: FALSE

# these are the default settings, these can be placed in the list above
default_on_final: TRUE
default_on_train: TRUE
default_every_n_steps: 1200
