metric_list:
  - flatness:
      every_n_steps: 110
  - factored_components:
      every_n_steps: 111
  - mig:
      every_n_steps: 112
  - sap:
      every_n_steps: 113
  - unsupervised:
      every_n_steps: 114
  - dci:
      every_n_steps: 115
  - factor_vae:
      every_n_steps: 116

# these are the default settings, these can be placed in the list above
default_on_final: FALSE
default_on_train: TRUE
default_every_n_steps: 200
default_begin_first_step: FALSE
