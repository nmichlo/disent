# @package _group_
name: conv64ws
weight_init: 'xavier_normal'
encoder:
  _target_: disent.model.ae.EncoderConv64Ws
  x_shape: ${dataset.x_shape}
  z_size: ${model.z_size}
  z_multiplier: ${framework.model_z_multiplier}
decoder:
  _target_: disent.model.ae.DecoderConv64Ws
  x_shape: ${dataset.x_shape}
  z_size: ${model.z_size}
