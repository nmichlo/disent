# @package _group_
name: simpleconv64
encoder:
  cls:
    target: disent.model.encoders_decoders.EncoderSimpleConv64
    params:
      x_shape: ${dataset.x_shape}
      z_size: ${model.z_size}
decoder:
  cls:
    target: disent.model.encoders_decoders.DecoderSimpleConv64
    params:
      x_shape: ${dataset.x_shape}
      z_size: ${model.z_size}