# @package _global_
sampler:
  name: default
  # this config forces an error to be thrown if
  # sampler config settings are required.

# CUSTOM SPECIALIZATION (hydra does not support variable interpolation in defaults)
# -- choose the default from the framework and dataset
specializations:
   dataset_sampler: ${dataset.data_type}__${framework.data_sample_mode}
