# @package _global_
callbacks:
  latent_cycle:
    seed: 7777
    every_n_steps: 600
    mode: 'minmax_interval_cycle'  # 'minmax_interval_cycle', 'fitted_gaussian_cycle'
  gt_dists:
    seed: 7777
    every_n_steps: 600
    traversal_repeats: 100
