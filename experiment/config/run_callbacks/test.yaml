# @package _global_

callbacks:
  latent_cycle:
    seed: 7777
    every_n_steps: 100
    mode: 'minmax_interval_cycle'  # 'minmax_interval_cycle', 'fitted_gaussian_cycle'
    begin_first_step: FALSE

  gt_dists:
    seed: 7777
    every_n_steps: 101
    traversal_repeats: 10
    begin_first_step: FALSE

  correlation:
    repeats_per_factor: 10
    every_n_steps: 102
