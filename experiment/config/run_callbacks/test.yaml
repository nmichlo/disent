# @package _global_
callbacks:
  progress:
    interval: 1
  latent_cycle:
    seed: 7777
    every_n_steps: 100
    mode: 'minmax_interval_cycle'  # 'minmax_interval_cycle', 'fitted_gaussian_cycle'
  metrics:
    every_n_steps: 101
  correlation:
    repeats_per_factor: 10
    every_n_steps: 102
