# @package _global_
callbacks:
  latent_cycle:
    seed: 7777
    every_n_steps: 1200
    mode: 'minmax_interval_cycle'  # 'minmax_interval_cycle', 'fitted_gaussian_cycle'
  correlation:
    repeats_per_factor: 10
    every_n_steps: 7200
