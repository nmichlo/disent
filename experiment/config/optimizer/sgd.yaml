# @package _global_
framework:
  module:
    optimizer: torch.optim.SGD
    optimizer_kwargs:
      lr: ${optimizer.lr}
      momentum: 0
      dampening: 0
      weight_decay: 0
      nesterov: False
