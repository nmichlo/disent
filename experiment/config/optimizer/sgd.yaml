# @package _global_

framework:
  cfg:
    optimizer: torch.optim.SGD
    optimizer_kwargs:
      lr: ${settings.optimizer.lr}
      momentum: 0
      dampening: 0
      weight_decay: 0
      nesterov: False
