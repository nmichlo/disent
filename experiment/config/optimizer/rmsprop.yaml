# @package _global_

framework:
  cfg:
    optimizer: torch.optim.RMSprop
    optimizer_kwargs:
      lr: ${settings.optimizer.lr}  # default was 1e-2
      alpha: 0.99
      eps: 1e-8
      weight_decay: 0

      momentum: 0
      centered: False
