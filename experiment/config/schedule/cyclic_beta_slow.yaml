# @package _global_
schedules:
  beta:
    _target_: disent.schedule.Cyclic
    period: 7200
    repeats: NULL
    r_start: 0.0
    r_end: 1.0
    end_value: 'end'  # start/end -- only used if repeats is set
    mode: 'linear'
