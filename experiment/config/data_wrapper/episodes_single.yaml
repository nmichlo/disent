# @package _group_
name: episode_single
wrapper:
  _target_: disent.dataset.episodes.RandomEpisodeDataset
  num_samples: 1
  # TODO: this needs to be updated to use the same API as ground_truth wrappers.
  sample_radius: 32
