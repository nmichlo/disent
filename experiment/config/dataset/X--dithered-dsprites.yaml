# @package _group_
name: dithered-dsprites
data:
  _target_: disent.dataset.wrapper.DitheredDataset
  gt_data:
    _target_: disent.dataset.data.DSpritesData
    data_root: ${dataset.data_root}
    prepare: True
    in_memory: ${dataset.try_in_memory}
  dither_n: 2
  keep_ratio: 0.5
transform:
  _target_: disent.nn.transform.ToStandardisedTensor
x_shape: [1, 64, 64]

data_type: random
