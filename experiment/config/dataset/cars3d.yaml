# @package _group_
cls:
  target: disent.dataset.ground_truth.data_cars3d.Cars3dData
  params:
    data_dir: ${dataset.data_dir}/cars3d
    force_download: False
transforms:
  - target: torchvision.transforms.Resize
    params:
      size: 64
  - target: torchvision.transforms.ToTensor
x_shape: [3, 64, 64]