# @package _group_
name: xygrid
cls:
  target: disent.dataset.ground_truth_data.data_xygrid.XYData
  params:
    grid_size: 64
    grid_spacing: 1
    square_size: 5
transforms:
  - target: torchvision.transforms.ToTensor
x_shape: [1, 64, 64]

