# @package _group_
cls:
  target: disent.dataset.ground_truth.data_shapes3d.Shapes3dData
  params:
    data_dir: ${dataset.data_dir}/3dshapes
    in_memory: ${dataset.try_in_memory}
    force_download: False
    force_preprocess: False
x_shape: [3, 64, 64]