# @package _group_
cls:
  target: disent.dataset.ground_truth.data_mpi3d.Mpi3dData
  params:
    subset: 'real'
    data_dir: ${dataset.data_dir}/mpi3d
    in_memory: ${dataset.try_in_memory}
    force_download: False
x_shape: [3, 64, 64]