# @package _group_
name: xymultigrid
data:
  _target_: disent.data.groundtruth.XYMultiGridData
  square_size: 8
  grid_size: 64
  grid_spacing: 8
  num_squares: 3
  rgb: TRUE
transforms:
  - _target_: torchvision.transforms.ToPILImage
  - _target_: torchvision.transforms.ToTensor
x_shape: [3, 64, 64]
# (floor((64-8) / 8) + 1) ** 6 == 262144  // 256 = 1024 batches per epoch