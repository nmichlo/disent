# @package _global_
dataset:
  sampler:
    name: gt_dist__pair
    cls:
      _target_: disent.dataset.sampling.GroundTruthDistSampler
      num_samples: 2
      triplet_sample_mode: ${dataset_sampler.cfg.triplet_sample_mode} # random, factors, manhattan, combined
      triplet_swap_chance: ${dataset_sampler.cfg.triplet_swap_chance}
