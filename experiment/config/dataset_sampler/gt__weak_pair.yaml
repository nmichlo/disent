# @package _global_
dataset:
  sampler:
    name: gt__weak_pair
    cls:
      _target_: disent.dataset.sampling.GroundTruthPairOrigSampler
      # factor sampling
      p_k: ${dataset_sampler.cfg.k.1}
