# @package _group_
name: ground_truth_pair
sampler:
  _target_: disent.dataset.sampling.GroundTruthPairSampler
  # factor sampling
  p_k_range: ${dataset_sampling.k}
  # radius sampling
  p_radius_range: ${dataset_sampling.k_radius}
