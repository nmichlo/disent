# @package _global_
dataset:
  sampler:
    name: gt__pair
    cls:
      _target_: disent.dataset.sampling.GroundTruthPairSampler
      # factor sampling
      p_k_range: ${dataset_sampler.cfg.k}
      # radius sampling
      p_radius_range: ${dataset_sampler.cfg.k_radius}
