# @package _global_

# call the listed functions here before the experiment is started
# - make sure we register the research code to disent so that it can be used!
experiment:
  plugins:
    - research.code.register_to_disent
