# This workflow will install Python dependencies,
# then run tests over a variety of Python versions.

name: tests

on: [pull_request]

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest] # [ubuntu-latest, windows-latest, macos-latest]
        # the library itself runs on 3.11-3.14, but this job installs `.[test]`,
        # which pulls `hydra-core==1.1.1` -> `omegaconf 2.1.x` -> `antlr4-python3-runtime 4.8`.
        # that antlr release imports `typing.io`, removed in python 3.13, so pytest cannot
        # even start. raise this to 3.14 once hydra is unpinned.
        python-version: ["3.11", "3.12"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v5
      with:
        python-version: ${{ matrix.python-version }}
        # -- caching actually slows down the action!
        # cache: 'pip'
        # cache-dependency-path: pyproject.toml

    - name: Install dependencies
      # torchsort (pulled in by the `extra` group) needs torch to already be installed
      run: |
        python3 -m pip install --upgrade pip
        python3 -m pip install "torch>=2.0.0"
        python3 -m pip install -e ".[test]"

    - name: Test with pytest
      run: |
        python3 -m pytest --cov=disent tests/

    - uses: codecov/codecov-action@v4
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        fail_ci_if_error: false
      # codecov automatically merges all generated files
      # if: matrix.os == 'ubuntu-latest' && matrix.python-version == 3.9
